# Node
node_modules/
dist/
npm-debug.log*
*.tgz
*-protocoll.txt

# Environment / secrets
.env
*.env
.env.*
!.env.example
!.env.sample
.npmrc
secrets.json
secret*.json
*secret*.json
credentials.json
credential*.json
*credential*.json
*.token
*.tokens
*token*.json
*recovery*code*
*recovery*codes*
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
*.der
.mcpregistry_github_token
.mcpregistry_registry_token
.mcpregistry_*_token

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Temporary locks & multi-agent coordination
LOCK
LOCK.*
LOCK*.txt
LOCK.user.*
LOCK.until.*
LOCK.condition.*
LOCK.permissions.json
*.lock
.automation-lock
!package-lock.json

# Multi-host sync conflict copies
*.conflict
*.sync-conflict-*
*.sync-temp-*
*-CONFLIT-*
*-conflict-*
*conflicted copy*
* (Kopie)*
* (Copy)*
* (kopie)*
* (copy)*
*-WORKSTATION*
*-WORKSTATION-LG*
*-LAPTOP*
*-ASUS*
*-ASUS-GEI*
*-Mac Studio*
*-MacBook*

# Temporary files, patch residues & editor backups
*.tmp
*.bak
*.swp
*.orig
*.rej
*~

# Test, coverage, and build caches
.pytest_cache/
.ruff_cache/
.coverage/
.coverage
.coverage.*
coverage/
htmlcov/
.wheel-smoke/
.hypothesis/
.nyc_output/
.turbo/
build/
